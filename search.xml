<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Linux笔记</title>
    <url>/2023/11/20/Linux%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p>随心笔记</p>
<span id="more"></span>

<p>学习完 <a href="https://www.bilibili.com/video/BV1n84y1i7td/?spm_id_from=333.999.0.0&vd_source=7cfb68109cfb644748c42234407548fb">黑马程序员新版Linux零基础快速入门到精通</a> 受益匪浅，老师讲了许多基础的语法知识，深入浅出的带我认识和理解了很多Linux的操作，我将它们整理下来供大家参考学习，也方便我后续的复习反刍。（该文章适合有一定基础的小伙伴学习）</p>
<h1 id="一-前言"><a href="#一-前言" class="headerlink" title="一. 前言"></a>一. 前言</h1><p><strong>包括以下：</strong></p>
<ul>
<li>系统阐述</li>
<li>初识Linux</li>
<li>虚拟机介绍</li>
<li>VM 和 CenOS的安装，配置环境</li>
</ul>
<p>以上我都不讲，有兴趣的可以去看上边的链接视频（问就是任性）</p>
<ul>
<li>Linux 目录结构<br><img src="/./%EF%BF%BD%EF%BF%BD%EF%BF%BDG/%5DE0%25XEQV4L8Y7WH%7D3NY13J.png"><br>Linux 目录结构是一个树形结构，不像 win 系统可以拥有多个盘符，Linux 只有根目录 <em>&#x2F;</em> ，所有文件都在 <strong>&#x2F;</strong> 下面。<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">/usr/.cocal/hello,txt</span><br><span class="line">第一个 / 是根目录</span><br><span class="line">后面的 / 表示层级关系</span><br></pre></td></tr></table></figure></li>
<li>快捷键<br>ctrl+c 强制停止<br>ctrl+d 退出或登出<br>ctrl + l 或 clear 命令 清空终端内容<br>history 显示历史命令<br>ctrl + r 搜索历史命令<br>!命令前缀，自动执行上一次匹配的命令</li>
</ul>
<h1 id="二-基础命令"><a href="#二-基础命令" class="headerlink" title="二. 基础命令"></a>二. 基础命令</h1><h2 id="1-ls-cd-pwd"><a href="#1-ls-cd-pwd" class="headerlink" title="1. ls - cd - pwd"></a>1. ls - cd - pwd</h2><ul>
<li>&amp; <strong>ls</strong></li>
</ul>
<p>** 列出目录下内容 **</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：ls [-a -l -h] [Linux 路径]</span><br><span class="line">-a : all 列出全部文件（包括隐藏文件/文件夹）</span><br><span class="line">-l : 以列表（竖向排序）展示内容</span><br><span class="line">-h : 以方便阅读的方式列出文件大小，K.M.G （一定要搭配 *-l* 才行）</span><br></pre></td></tr></table></figure>

<ul>
<li>&amp; <strong>cd</strong></li>
</ul>
<p><strong>切换到哪个目录下</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//change directory</span></span><br><span class="line">语法：cd [Linux 路径]</span><br></pre></td></tr></table></figure>
<ol>
<li>cd 命令没有选项,只有参数,表示切换到哪个目录下</li>
<li>cd 命令直接执行,不带参数,返回用户HOME目录</li>
</ol>
<ul>
<li>&amp; <strong>pwd</strong><br><strong>查看当前工作目录</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//print work directory</span></span><br><span class="line">语法: pwd</span><br><span class="line">pwd 命令,无选项,无参数,直接输入表示查看当前工作目录</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="2-相对路径与绝对路径-特殊路径符"><a href="#2-相对路径与绝对路径-特殊路径符" class="headerlink" title="2. 相对路径与绝对路径 - 特殊路径符"></a>2. 相对路径与绝对路径 - 特殊路径符</h2><ul>
<li><p>&amp; <strong>相对路径和绝对路径</strong><br>绝对路径：以<strong>根目录</strong>为起点，描述路径，以 <em>&#x2F;</em> 开头<br>相对路径：以<strong>当前目录</strong>为起点，描述路径</p>
</li>
<li><p>&amp; <strong>特殊路径符</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">.    表示当亲目录</span><br><span class="line">..   表示上一级目录，cd .. 表示回退到上一级， cd ../.. 表示回退上二级目录</span><br><span class="line">~    表示HOME目录，cd ~ 表示切换到HOME目录</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="3-mkdir-touch"><a href="#3-mkdir-touch" class="headerlink" title="3. mkdir - touch"></a>3. mkdir - touch</h2><ul>
<li><p>&amp; <strong>mkdir</strong><br><strong>创建新的文件夹（目录）</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//make directory</span></span><br><span class="line">语法：mkdir [-p] Linux路径</span><br><span class="line">-p：自动创建不存在的父目录，适用于创建连续多级目录（可选）</span><br></pre></td></tr></table></figure>
<p><strong>注</strong>：创建文件夹有时会涉及权限问题，尽量创建文件于HOME目录内。</p>
</li>
<li><p>&amp; <strong>touch</strong><br><strong>创建文件</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：touch Linux路径</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="4-cat-more-vi-vim-编辑器-tail"><a href="#4-cat-more-vi-vim-编辑器-tail" class="headerlink" title="4. cat - more - vi&#x2F;vim 编辑器 - tail"></a>4. cat - more - vi&#x2F;vim 编辑器 - tail</h2><ul>
<li><p>&amp; <strong>cat</strong><br><strong>查看文件内容</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：cat Linux路径</span><br><span class="line">cat 无选项 参数必填</span><br></pre></td></tr></table></figure>
<p><strong>注：</strong> 会展示全部文件内容，不适合过大的文件查看。</p>
</li>
<li><p>&amp; <strong>more</strong><br><strong>查看文件内容</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：more Linux路径</span><br><span class="line">cat 无选项 参数必填</span><br></pre></td></tr></table></figure>
<p>空格翻页，q 退出。<br><strong>注：</strong> 支持翻页，适合大文件查看。</p>
</li>
<li><p>&amp; <strong>vi &#x2F; vim编辑器</strong><br><strong>查看文件内容</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//visual interface</span></span><br><span class="line">语法：vi / vim 文件路径</span><br></pre></td></tr></table></figure>
<p>vi &#x2F; vim 编辑器拥有三种模式：命令模式，输入模式，底线命令模式。<br><img src="/./%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/40~I3TZEQGRSW7%7DBV04JY7U.png"></p>
</li>
</ul>
<ol>
<li>三种模式不能随意交换，输入模式与底线命令模式不相通</li>
<li>如果文件路径的文件<strong>不存在</strong>，该命令会创建新文件夹</li>
<li>如果文件路径的文件<strong>存在</strong>，该命令会编辑已有的文件</li>
<li>vim 是 vi 的加强版兼容所有 vi 功能，建议用新的 vim 命令</li>
</ol>
<p><em>命令模式快捷键</em>（vim 命令直接进入）<br><img src="/./%EF%BF%BD%EF%BF%BD%EF%BF%BDG./CA%7D442@OQ$K%25KJY5D%5B%25~IF.png"><br><img src="/./%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/E6H5QU15M78FJ$YWJD.png"></p>
<p><em>底线命令模式</em>（在命令模式下输入 <strong>:</strong> 进入）<br><img src="/./%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/49X9FP33H5HE92WK0.png"></p>
<ul>
<li>&amp; <strong>tail</strong><br><strong>查看文件尾部内容，跟踪文件最新更改</strong></li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：tail [-f -num] Linux路径</span><br><span class="line">-f： 持续跟踪</span><br><span class="line">-num： 查看尾部多少行，默认<span class="number">10</span>行</span><br></pre></td></tr></table></figure>

<p>带上 -f 后程序会跟踪文件最新更改，&#x3D;&#x3D;ctrl + c&#x3D;&#x3D;停止</p>
<h2 id="5-cp-mv-rm"><a href="#5-cp-mv-rm" class="headerlink" title="5. cp - mv - rm"></a>5. cp - mv - rm</h2><ul>
<li><p>&amp; <strong>cp</strong><br><strong>复制文件&#x2F;文件夹</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//copy</span></span><br><span class="line">语法：cp [-r] 参<span class="number">1</span> 参<span class="number">2</span></span><br><span class="line">-r：复制对象是文件夹</span><br><span class="line">参<span class="number">1</span>：被复制的文件/文件路径</span><br><span class="line">参<span class="number">2</span>：要复制去的地方</span><br></pre></td></tr></table></figure>
<p><strong>注：</strong>参2路径的文件&#x2F;文件夹如果不存在的话，系统会自动创建<strong>参2文件&#x2F;文件夹</strong></p>
</li>
<li><p>&amp; <strong>mv</strong><br><strong>移动文件&#x2F;文件夹，修改名称</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//move</span></span><br><span class="line">语法：mv 参<span class="number">1</span> 参<span class="number">2</span></span><br><span class="line">参<span class="number">1</span>：被复移动的文件/文件路径</span><br><span class="line">参<span class="number">2</span>：要移动去的地方，或要更改的名称</span><br></pre></td></tr></table></figure>
<p><strong>注：</strong>参2路径的文件&#x2F;文件夹如果不存在的话，系统会自动<strong>更改参1的名称</strong></p>
</li>
<li><p>&amp; <strong>rm</strong><br><strong>删除文件&#x2F;文件夹</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//remove</span></span><br><span class="line">语法：rm [-r -f] n个参数</span><br><span class="line">-r：删除对象为文件夹</span><br><span class="line">-f：force，强制删除</span><br><span class="line">参数：要删除的文件/文件夹路径，可以同时删除多个，以空格隔开</span><br></pre></td></tr></table></figure>
<p><strong>注：</strong> rm 是一个&#x3D;&#x3D;危险&#x3D;&#x3D;的命令，尤其是处于root（超级管理员）用户下。</p>
</li>
</ul>
<h2 id="6-which-find"><a href="#6-which-find" class="headerlink" title="6. which - find"></a>6. which - find</h2><ul>
<li><p>&amp; <strong>which</strong><br><strong>查看命令的程序文件存放位置</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：which 命令</span><br></pre></td></tr></table></figure>
<p>Linux命令本质就是一个个可执行程序，就像自定义函数，使用它就去调用它，我们使用的命令都是系统自带的，which 命令可以找到该命令的位置。</p>
</li>
<li><p>&amp; <strong>find</strong><br><strong>按文件名查找文件</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法<span class="number">1</span>：find 起始地址 -name <span class="string">&quot;文件名&quot;</span></span><br><span class="line">起始地址：文件从哪开始查找</span><br><span class="line">-name：表示查找文件名</span><br><span class="line"></span><br><span class="line">语法<span class="number">2</span>：find 起始地址 -size +/- n [k/M/G]</span><br><span class="line">-size：按文件大小查找</span><br><span class="line">+/-：表示大于或小于</span><br><span class="line">n：表示大小数字</span><br><span class="line">k/M/G：表示单位</span><br></pre></td></tr></table></figure>
<p>例：<br>在<strong>HOME目录</strong>下查找名字<strong>带有 test</strong> 的文件：<code>find home -name &quot;*test*&quot;</code><br>在<strong>根目录</strong>下查找名字<strong>带有 test</strong> 的文件：<code>find / -name &quot;*test*&quot;</code><br>在<strong>根目录</strong>查找<strong>小于10kb 大小</strong>的文件：<code>find / -size -10k</code><br>在<strong>根目录</strong>查找<strong>大于10GB</strong> 大小的文件：<code>find / -size +10G</code></p>
</li>
</ul>
<h2 id="7-grep-wc"><a href="#7-grep-wc" class="headerlink" title="7. grep - wc"></a>7. grep - wc</h2><ul>
<li><p>&amp; <strong>grep</strong><br><strong>通过关键字过滤文件行</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：grep [-n] <span class="string">&quot;关键字&quot;</span> 文件路径</span><br><span class="line">-n：在结果中显示行号</span><br><span class="line">关键字：必填，需要隔离的关键字</span><br></pre></td></tr></table></figure>
</li>
<li><p>&amp; <strong>wc</strong><br><strong>统计文件行数，单词数量等</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：wc [-c -m -l -w] 文件路径</span><br><span class="line">-c：计算文件大小单位byte</span><br><span class="line">-m：统计字符串数量</span><br><span class="line">-l：统计行数</span><br><span class="line">-w：统计单词数量</span><br></pre></td></tr></table></figure>
<p>不带选项，默认输出 -l，-w，-c 和文件名。</p>
</li>
</ul>
<h2 id="8-重定向符-管道符-通配符"><a href="#8-重定向符-管道符-通配符" class="headerlink" title="8. 重定向符 - 管道符 -通配符"></a>8. 重定向符 - 管道符 -通配符</h2><ul>
<li>&amp; <strong>重定向符</strong></li>
</ul>
<p><strong>&gt;</strong>      将左侧命令的结果，<em>覆盖</em>写入符号右侧指定的文件中<br><strong>&gt;&gt;</strong>    将左侧命令的结果，<em>追加</em>写入符号右侧指定的文件中</p>
<p><strong>注：</strong> 如果左侧为<strong>命令</strong>，那么会把命令产生的结果覆盖或追加到指定文件中</p>
<ul>
<li>&amp; <strong>管道符</strong></li>
</ul>
<p><strong>|</strong> 含义是将左边命令的结果作为右边命令的输入。<br>例如：<br>统计test.txt文件中带有test关键字有几行：<code>cat test.txt | grep test | wc -l</code></p>
<p>适合搭配** cat grep** 等命令使用。</p>
<ul>
<li>&amp; <strong>通配符</strong><br><strong>模糊匹配</strong></li>
</ul>
<ol>
<li>test* , 表示如何任何以test开头的内容。</li>
<li>*test，表示如何任何以test结尾的内容。</li>
<li><em>test</em>，表示如何任何包含test的内容。</li>
</ol>
<p>适合搭配 <strong>rm find</strong> 等命令使用。</p>
<h2 id="9-echo"><a href="#9-echo" class="headerlink" title="9. echo"></a>9. echo</h2><ul>
<li>&amp; <strong>echo</strong><br><strong>打印内容</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：echo 输出的内容</span><br><span class="line">echo 无选项 参数必填</span><br></pre></td></tr></table></figure>
等同于c语言命令中的 <strong>printf</strong> ，打印内容在屏幕上</li>
</ul>
<ol>
<li>输出内容建议用双引号括起来，更加美观，容易辨识</li>
<li>输出内容为<strong>命令</strong>的话必须用 &#x3D;&#x3D;&#96;&#96;&#x3D;&#x3D; 括起来</li>
</ol>
<h1 id="三-权限"><a href="#三-权限" class="headerlink" title="三. 权限"></a>三. 权限</h1><h2 id="su-sudo"><a href="#su-sudo" class="headerlink" title="su - sudo"></a>su - sudo</h2><ul>
<li>&amp; <strong>su</strong><br><strong>切换用户</strong></li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//switch user</span></span><br><span class="line">语法：su - [用户名]</span><br><span class="line"><span class="built_in">exit</span> 命令会回退到上一个用户</span><br></pre></td></tr></table></figure>
<ol>
<li>root 用户拥有最大的系统操作权限，而普通用户在许多地方权限受限。</li>
<li>切换到root用户会要求输入密码，密码不会显现</li>
<li>普通用户一般会在HOME目录下不受影响</li>
<li>不填用户名默认为root用户<br><strong>注：</strong>权限越大，操作带来的影响就越危险</li>
</ol>
<ul>
<li>&amp; <strong>sudo</strong><br><strong>以root用户，为命令临时授权</strong><br><strong>注：</strong>该命令要先在认证文件中添加语句，不然无法使用</li>
</ul>
<p><img src="/"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：sudo 其他命令</span><br></pre></td></tr></table></figure>


<h2 id="用户与用户组"><a href="#用户与用户组" class="headerlink" title="用户与用户组"></a>用户与用户组</h2><p>Linux关于权限管理级别有两个</p>
<ol>
<li>针对<strong>用户</strong>的权限控制</li>
<li>针对<strong>用户组</strong>的权限控制</li>
</ol>
<ul>
<li><p>&amp; <strong>groupadd</strong><br><strong>创建用户组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">groupadd 组名</span><br></pre></td></tr></table></figure></li>
<li><p>&amp; <strong>grupdel</strong><br><strong>删除用户组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">grupdel 组名</span><br></pre></td></tr></table></figure>
</li>
<li><p>&amp; <strong>useradd</strong><br><strong>创建用户</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：useradd [-g -d] 用户名</span><br><span class="line">-g：指定用户组，不指定会创建同名组并且自动加入</span><br><span class="line">-d：指定用户HOME路径，默认在/home/用户名</span><br></pre></td></tr></table></figure>
</li>
<li><p>&amp; <strong>userdel</strong><br><strong>删除用户</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">userdel [-r] 用户名</span><br><span class="line">-r：删除用户的HOME目录，不带则保留</span><br></pre></td></tr></table></figure>
</li>
<li><p>&amp; <strong>id</strong><br><strong>查看用户所属的组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：id 用户名</span><br><span class="line">用户名：查看的用户，不带则查看自身</span><br></pre></td></tr></table></figure>
</li>
<li><p>&amp; <strong>usermod -aG</strong><br><strong>修改用户所属的组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：usermod -aG 用户组 用户</span><br></pre></td></tr></table></figure>
<p>将指定用户加入指定用户组</p>
</li>
<li><p>&amp; <strong>getent</strong><br><strong>查看该电脑中有哪些用户或用户组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：getent passwd[用户]/group[用户组]</span><br></pre></td></tr></table></figure>
<p>用户名带有七份信息：密码（x）；用户ID；描述信息；HOME目录；执行终端</p>
</li>
</ul>
<p>用户名带有三份信息：组名；密码；用户组ID</p>
<h2 id="查看权限控制信息"><a href="#查看权限控制信息" class="headerlink" title="查看权限控制信息"></a>查看权限控制信息</h2><p><img src="/"></p>
<h2 id="chmod-命令和chown-命令"><a href="#chmod-命令和chown-命令" class="headerlink" title="chmod 命令和chown 命令"></a>chmod 命令和chown 命令</h2><ul>
<li><p>&amp; <strong>chmod</strong><br><strong>修改文件&#x2F;文件夹的权限信息</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：chmod [-R] 权限 文件/文件夹</span><br><span class="line">-R：对文件内全部内容进行相同操作</span><br></pre></td></tr></table></figure>
<p><strong>注意：</strong>只有文件、文件夹所属用户或者root用户才可以使用该命令<br>快捷修改权限可以使用<strong>751权限</strong><br><img src="/"><br><strong>例如：</strong><br>将文件夹里所有文件权限修改为：rwx r-x –x 命令为：chmod -R u&#x3D;rwx,g&#x3D;rx,o&#x3D;x test<br>将文件夹权限修改为：rwx r-x –x 命令为：chmod 751 test<br><img src="/"></p>
</li>
<li><p>&amp; <strong>chown</strong><br><strong>可以修改文件&#x2F;文件夹所属用户和用户组</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：chown [-R] [用户][:][用户组] 文件/文件夹</span><br><span class="line">-R：对文件内全部内容进行相同操作</span><br><span class="line">: ：分隔</span><br></pre></td></tr></table></figure>
<p><strong>注意：</strong>该命令只有root用户可以使用<br><strong>例如：</strong><br>将文件夹里所有文件权限修改为root用户，用户组修改为kkfp：<br>chown -R root:kkdp test</p>
</li>
</ul>
<h2 id="yum-软件安装"><a href="#yum-软件安装" class="headerlink" title="yum 软件安装"></a>yum 软件安装</h2><ul>
<li>&amp; <strong>yum</strong><br><strong>rpm包软件管理器，用于自动化安装配置Linux软件</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：yum [-y] [install | remove | sratch] 软件名称</span><br><span class="line">-y：自动确认</span><br><span class="line">install 安装，remove 卸载，search 搜索</span><br></pre></td></tr></table></figure>
<strong>注意：</strong> 该命令需要root权限和联网</li>
</ul>
<h2 id="systemctl-命令"><a href="#systemctl-命令" class="headerlink" title="systemctl 命令"></a>systemctl 命令</h2><ul>
<li>&amp; <strong>systemctl</strong><br><strong>管理服务</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">systemctl start | stop | status | enable | disable 服务名</span><br><span class="line">start 启动，stop 关闭，status 查看状态，enable 开机启动，disable 关闭开机启动</span><br></pre></td></tr></table></figure>
<strong>注意：</strong>部分软件安装后没有自动集成到systemctl中，这些需要手动添加</li>
</ul>
<p>内置服务有很多，比如：<br>networkmanger 主网络服务<br>network 副网络服务<br>firewalld 防火墙服务<br>sshd ssh服务</p>
<h2 id="ln"><a href="#ln" class="headerlink" title="ln"></a>ln</h2><ul>
<li>&amp; <strong>ln</strong><br><strong>软连接：在系统中创建软连接可以将文件链接到其他位置</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：ln -s 参<span class="number">1</span> 参<span class="number">2</span></span><br><span class="line">-s：创建软连接</span><br><span class="line">参<span class="number">1</span>：被链接的文件或文件夹</span><br><span class="line">参<span class="number">2</span>：要链接去的目的地</span><br></pre></td></tr></table></figure>
类似于win下的创建<strong>快捷方式</strong></li>
</ul>
<h2 id="ping-wget-curl"><a href="#ping-wget-curl" class="headerlink" title="ping - wget - curl"></a>ping - wget - curl</h2><ul>
<li><p>&amp; <strong>ping</strong><br><strong>查看指定网络服务器是否联通</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：ping [-c num] ip或主机名</span><br><span class="line">-c：检查的次数，不填默认无限次检查</span><br></pre></td></tr></table></figure></li>
<li><p>&amp; <strong>wget</strong><br><strong>非交换式文件下载器，可下载网络文件</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：wget [-b] url</span><br><span class="line">-b：可选，后台下载</span><br><span class="line">url：链接</span><br></pre></td></tr></table></figure>

</li>
<li><p>&amp; <strong>curl</strong><br><strong>可下载文件也可以发送网络请求</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：curl [-O] url</span><br><span class="line">-O：用于下载文件，当url为下载链接时，可以使用此项目保存文件</span><br><span class="line">url：链接</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="ps-kill"><a href="#ps-kill" class="headerlink" title="ps - kill"></a>ps - kill</h2><ul>
<li>&amp; <strong>ps</strong><br><strong>查看Linux系统中的进程信息</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：ps [-e -f]</span><br><span class="line">-e：显示全部的进程</span><br><span class="line">-f：以完全格式化的形式展示信息</span><br><span class="line">一般是固定语法 ps -ef</span><br></pre></td></tr></table></figure></li>
<li>&amp; <strong>kill</strong><br><strong>关闭进程</strong><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">语法：kill [<span class="number">-9</span>] 进程ID</span><br><span class="line"><span class="number">-9</span>：强制关闭进程</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="主机状态监控"><a href="#主机状态监控" class="headerlink" title="主机状态监控"></a>主机状态监控</h2><ul>
<li>&amp; <strong>top</strong></li>
</ul>
<h2 id="环境变量"><a href="#环境变量" class="headerlink" title="环境变量"></a>环境变量</h2><h2 id="文件的上传下载"><a href="#文件的上传下载" class="headerlink" title="文件的上传下载"></a>文件的上传下载</h2><h2 id="压缩与解压"><a href="#压缩与解压" class="headerlink" title="压缩与解压"></a>压缩与解压</h2><h1 id="实战"><a href="#实战" class="headerlink" title="实战"></a>实战</h1><ul>
<li>未完待续</li>
</ul>
]]></content>
      <categories>
        <category>Linux笔记</category>
      </categories>
      <tags>
        <tag>c语言 算法</tag>
      </tags>
  </entry>
  <entry>
    <title>leetcode 中二分查找的((right - left) &gt;&gt; 1) + left</title>
    <url>/2023/11/10/leetcode-%E4%B8%AD%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE%E7%9A%84-right-left-1-left/</url>
    <content><![CDATA[<p>随心笔记</p>
<span id="more"></span>



<p>官方原题为：</p>
<p><img src="/./%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/L.png" alt="1"></p>
<p>该题官方原解为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">searchInsert</span><span class="params">(<span class="type">int</span>* nums, <span class="type">int</span> numsSize, <span class="type">int</span> target)</span> &#123;</span><br><span class="line">    <span class="type">int</span> left = <span class="number">0</span>, right = numsSize - <span class="number">1</span>, ans = numsSize;</span><br><span class="line">    <span class="keyword">while</span> (left &lt;= right) &#123;</span><br><span class="line">        <span class="type">int</span> mid = ((right - left) &gt;&gt; <span class="number">1</span>) + left;</span><br><span class="line">        <span class="keyword">if</span> (target &lt;= nums[mid]) &#123;</span><br><span class="line">            ans = mid;</span><br><span class="line">            right = mid - <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            left = mid + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中这段代码，有点小疑惑</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> mid = ((right - left) &gt;&gt; <span class="number">1</span>) + left;</span><br></pre></td></tr></table></figure>
<p>这段代码可以同理为（关于二分查找的详细步骤我这里不赘述了）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> mid = (right + left) / <span class="number">2</span>;</span><br></pre></td></tr></table></figure>
<p>但是我们知道<strong>int</strong>是有范围的，如果<strong>r + l</strong>过大会溢出，所以该代码健壮性较差。<br><strong>注意：</strong> 当位移运算符前数字为<strong>偶数</strong>时， <strong>&gt;&gt; 1</strong> 等同 <strong>&#x2F; 2</strong> 。</p>
<p>这段代码有两个好处：</p>
<ul>
<li><strong>(r - l)</strong> 不会溢出，因为我们知道 <strong>r &gt; l</strong> 。</li>
<li><strong>(&gt;&gt; 1)</strong> 位移运算符所要的计算量少，而且可以代替 <strong>&#x2F; 2</strong> 。</li>
</ul>
]]></content>
      <categories>
        <category>c语言笔记</category>
      </categories>
      <tags>
        <tag>c语言 算法</tag>
      </tags>
  </entry>
  <entry>
    <title>结构体大小计算</title>
    <url>/2023/10/23/%E7%BB%93%E6%9E%84%E4%BD%93%E5%A4%A7%E5%B0%8F%E8%AE%A1%E7%AE%97/</url>
    <content><![CDATA[<p>结构体内存大小计算不是简单的 1 + 1 ，那么系统是如何存放结构体的数据？它的大小又是如何计算的呢？</p>
<span id="more"></span>


<h2 id="简单结构体大小计算"><a href="#简单结构体大小计算" class="headerlink" title="简单结构体大小计算"></a>简单结构体大小计算</h2><p>可以简单分为<strong>三步走战略</strong>：</p>
<ol>
<li><p>找到各个成员大小，这个CPU原因会有所差异。</p>
</li>
<li><p>考虑成员对其方式<br><strong>注：</strong> 这里会有一个默认对其数的概念，如VS编译器默认对其数为8。<br>找到最小值</p>
</li>
</ol>
<p><img src="/.%5C%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87%5Cqqq.png"></p>
<ol start="3">
<li>计算结构体的总大小<br> 把它在内存中的存在方式画出来</li>
</ol>
<p>  <img src="/.%5C%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87%5Cwww.png"></p>
<p>我们可以清楚的看到该结构体的大小为13</p>
<h2 id="进阶大小计算"><a href="#进阶大小计算" class="headerlink" title="进阶大小计算"></a>进阶大小计算</h2><ol>
<li>结构体数组大小计算</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Student1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="type">double</span> num;</span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line">	<span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>首先找最小值：<br>double -&gt; 8（内存大小） -&gt; 8（默认对齐数） -&gt; 8（最小值）<br>char -&gt; 1 -&gt; 8 -&gt;1<br>int -&gt; 4 -&gt; 8 -&gt; 4<br>考虑对其方式：<br>这个 char 类型的数组，只需要把它看做十个 char 型连在一起即可，最大对齐数是 int 的4<br>计算：<br>所以该结构体的大小为：8 + 10 + 空2 + 4 &#x3D; 24</p>
<ol>
<li>嵌套结构体大小计算<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">s1</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> a;<span class="comment">//1 -&gt; 8 -&gt; 1</span></span><br><span class="line">    <span class="type">int</span> b;<span class="comment">//4 -&gt; 8 -&gt; 4</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">s</span> &#123;</span></span><br><span class="line">        <span class="type">char</span> c;<span class="comment">//1 -&gt; 8 -&gt; 1 </span></span><br><span class="line">        <span class="type">int</span> d;<span class="comment">//4 -&gt; 8 -&gt; 4</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="type">float</span> e;<span class="comment">//4 -&gt; 8 -&gt; 4</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
计算结构体大小时是把里面这个结构体就看做是一个char,和一个int，不是看做一个整体。<br>所以该结构体大小为：1 + 空3 + 4 +1 +空3 + 4 + 4 &#x3D; 20</li>
</ol>
<h2 id="修改默认对其数"><a href="#修改默认对其数" class="headerlink" title="修改默认对其数"></a>修改默认对其数</h2><p>我们可以借助预处理命令来修改‘</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> pack()</span></span><br></pre></td></tr></table></figure>
<p>C语言中的默认对齐数，取决于编译器和计算机体系结构。C语言标准并没有规定特定的默认对齐数，而是留给了编译器实现来确定。在大多数情况下，编译器会根据目标体系结构的要求来确定默认的对齐数。</p>
<p><strong>注意以下几点：</strong></p>
<ol>
<li><p>使用 #pragma pack() 可能会<strong>降低内存</strong>访问效率，因为不再按照默认的对齐方式进行对齐，导致内存访问更频繁。</p>
</li>
<li><p>使用不恰当的对齐值可能会导致与硬件或其他软件的<strong>兼容性问题</strong>。</p>
</li>
<li><p>在设置 #pragma pack(n) 后，通常应该在后续的代码中使用 #pragma pack() <strong>恢复默认的对齐方式</strong>，以免影响其他部分的代码。</p>
</li>
<li><p>使用 #pragma pack() 是非标准的，不具有跨编译器的可移植性，<strong>不同编译器可能会采用不同的语法或默认对齐值</strong>。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>c/c++基础语法</category>
      </categories>
      <tags>
        <tag>c语言 算法</tag>
      </tags>
  </entry>
  <entry>
    <title>你了解结构体吗？</title>
    <url>/2023/10/18/%E4%BD%A0%E4%BA%86%E8%A7%A3%E7%BB%93%E6%9E%84%E4%BD%93%E5%90%97/</url>
    <content><![CDATA[<p>​	我们知道c语言内置了许多基本数据类型，现在我需要一个可以同时存储多个不一样的数据类型的容器，比如：在小明这个变量下同时存在年龄，班级，成绩等数据，这些数据类型都不同但是他们又是表示一个整体，要存在联系，我该怎么把他们整合起来呢?</p>
<span id="more"></span>

<h2 id="啥是结构体"><a href="#啥是结构体" class="headerlink" title="啥是结构体"></a>啥是结构体</h2><p>  结构体是C语言中一种重要的数据类型，该数据类型由一组称为成员的数据所组成，其中每个成员可以是不同类型的。结构体通常用来表示类型不同但是又相关的若干数据。</p>
<p>  int、float、char 等是由C语言提供的数据类型，不能再进行分拆，我们称之为基本数据类型；而结构体可以包含多个基本类型的数据，也可以包含其他的结构体，我们将它称为<strong>复杂数据类型</strong>或<strong>构造数据类型</strong>。</p>
<h2 id="结构体的声明，定义和初始化"><a href="#结构体的声明，定义和初始化" class="headerlink" title="结构体的声明，定义和初始化"></a>结构体的声明，定义和初始化</h2><ul>
<li>在使用结构体之前我们应当先声明它，定义形式为</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">study</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="comment">//可以包含多个变量或数组，它们的类型可以相同，也可以不同</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p><strong>注意：</strong></p>
<ol>
<li>该结构体只是定义，还未初始化，所以不占用内存</li>
<li>如果结构体声明在maim{}外，那么该结构体变量是一个全局变量；同理，反之则为局部变量</li>
</ol>
<p>我们可以在对结构体定义的同时初始化</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Student</span>//结构名</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>];<span class="comment">//姓名</span></span><br><span class="line">	<span class="type">int</span> age;<span class="comment">//年龄</span></span><br><span class="line">	<span class="type">int</span> strudentid;<span class="comment">//学号</span></span><br><span class="line">	<span class="type">char</span> sex[<span class="number">10</span>];<span class="comment">//性别</span></span><br><span class="line">	<span class="type">float</span> score;<span class="comment">//成绩</span></span><br><span class="line">&#125; student = &#123;<span class="string">&quot;xiaoming&quot;</span>, <span class="number">20</span>, <span class="number">8997</span>, <span class="string">&#x27;男&#x27;</span>, <span class="number">98.5</span>&#125;;<span class="comment">//变量名</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name: %s\n age: %d\n studenid: %s\n sex: %d\n score: %f&quot;</span>,</span><br><span class="line"> student.name, student.age, student.studenid, student.sex, 			student.score);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<ul>
<li>如果大家觉得结构名太长可以用<strong>typedef</strong>来定义</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="comment">//...</span></span><br><span class="line">&#125;stu;</span><br></pre></td></tr></table></figure>



<p><code>stu</code><strong>在这里不是作为变量，而是为</strong><code>student</code><strong>的代称，你可以以操作</strong><code>student</code><strong>的方式来操作</strong><code>stu</code></p>
<h2 id="匿名结构体类型"><a href="#匿名结构体类型" class="headerlink" title="匿名结构体类型"></a>匿名结构体类型</h2><p>  顾名思义“匿名”就是改个昵称，但对于结构体而言，匿名就是没有名字</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="comment">//...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



<p>因为我们没有完全的声明，也就是不写结构体标签，所以称之为匿名</p>
<p><strong>注意：</strong> 这种语法c语言是支持的，但我们实际生活中一般不会用，因为该结构体只能用一次</p>
<h2 id="结构体指针"><a href="#结构体指针" class="headerlink" title="结构体指针"></a>结构体指针</h2><p>其实结构体的指针与基础数据类型的指针大同小异</p>
<p>例如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>];<span class="comment">//姓名</span></span><br><span class="line">	<span class="type">int</span> age;<span class="comment">//年龄</span></span><br><span class="line">	<span class="type">int</span> studentid;<span class="comment">//学号</span></span><br><span class="line">	<span class="type">char</span> sex[<span class="number">10</span>];<span class="comment">//性别</span></span><br><span class="line">	<span class="type">float</span> score;<span class="comment">//成绩</span></span><br><span class="line">&#125; student = &#123;<span class="string">&quot;xiaoming&quot;</span>, <span class="number">20</span>, <span class="number">8997</span>, <span class="string">&#x27;男&#x27;</span>, <span class="number">98.5</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//结构体指针</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Student</span> *<span class="title">stu</span> =</span> &amp;student;</span><br></pre></td></tr></table></figure>



<p>该程序把结构体变量<code>student</code>的地址赋给指针<code>Student</code><br><strong>注意：</strong> <strong>结构体和结构体变量是不一样的</strong>，我之前提到，声明只是画了一个结构图，就像一个肚子没墨水的花花公子，当然系统也没有给他分配内存，只有初始化之后（比如读了我的博客后）变成结构变量（有学识的人），才会有内存发配的，所以<strong>结构体是不能直接取地址的</strong></p>
<p>以下是错误示范：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Student</span> *<span class="title">stu</span> =</span> &amp;Student</span><br></pre></td></tr></table></figure>



<h2 id="结构体传参"><a href="#结构体传参" class="headerlink" title="结构体传参"></a>结构体传参</h2><p>同基本数据类型一样，数据结构也有两种传递方式，一种是传值，还有一种是传地址，那么到底是传值还是传地址好勒？</p>
<p>如果你对指针比较了解，我想你应该毫不犹豫选择指针，为什么？</p>
<p>因为在数据传递时，如果数据块较大（比如说数据缓冲区或比较大的结构体），这时就可以使用指针传递地址而不是实际数据，即提高传输速度，又节省大量内存。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">S</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"> <span class="type">int</span> x1;</span><br><span class="line"> <span class="type">int</span> x2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//结构体值传参</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">print1</span><span class="params">(<span class="keyword">struct</span> S s)</span></span><br><span class="line">&#123;</span><br><span class="line"> 	<span class="built_in">printf</span>(<span class="string">&quot;%d %d\n&quot;</span>, s.x1, s.x2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//结构体地址传参</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">print2</span><span class="params">(<span class="keyword">struct</span> S *p)</span></span><br><span class="line">&#123;</span><br><span class="line">	 <span class="built_in">printf</span>(<span class="string">&quot;%d %d\n&quot;</span>, p-&gt;x1, p-&gt;x2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">	 <span class="class"><span class="keyword">struct</span> <span class="title">S</span> <span class="title">s</span> =</span> &#123;<span class="number">10</span>, <span class="number">20</span>&#125;;</span><br><span class="line">	 print1(s);  <span class="comment">//传结构体值</span></span><br><span class="line">	 print2(&amp;s); <span class="comment">//传地址</span></span><br><span class="line">	 <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>结果：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span> <span class="number">20</span></span><br><span class="line"><span class="number">10</span> <span class="number">20</span></span><br></pre></td></tr></table></figure>

<p><strong>小知识：</strong> 如果传的为值那么我们用<code>.</code>来访问结构体成员；如果传地址的话我们就用<code>-&gt;</code>来访问，<code>-&gt;</code>也只能在结构体中使用。</p>
<h2 id="结构体数组"><a href="#结构体数组" class="headerlink" title="结构体数组"></a>结构体数组</h2><p>所谓结构题数组，就相当于结构题中每个成员都是数组</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="type">char</span> name[<span class="number">10</span>];<span class="comment">//姓名</span></span><br><span class="line">	<span class="type">int</span> age;<span class="comment">//年龄</span></span><br><span class="line">	<span class="type">int</span> studentid;<span class="comment">//学号</span></span><br><span class="line">	<span class="type">char</span> sex[<span class="number">10</span>];<span class="comment">//性别</span></span><br><span class="line">	<span class="type">float</span> score;<span class="comment">//成绩</span></span><br><span class="line">&#125; <span class="class"><span class="keyword">class</span>[] =</span> &#123; </span><br><span class="line">	&#123;<span class="string">&quot;xiaoming&quot;</span>, <span class="number">20</span>, <span class="number">8997</span>, <span class="string">&#x27;男&#x27;</span>, <span class="number">98.5</span>&#125;;</span><br><span class="line">	&#123;<span class="string">&quot;zhangsan&quot;</span>, <span class="number">21</span>, <span class="number">8998</span>, <span class="string">&#x27;男&#x27;</span>, <span class="number">78.5</span>&#125;</span><br><span class="line">	&#123;<span class="string">&quot;lisi&quot;</span>, <span class="number">20</span>, <span class="number">8999</span>, <span class="string">&#x27;女&#x27;</span>, <span class="number">88</span>&#125;	</span><br><span class="line">&#125;;		</span><br></pre></td></tr></table></figure>
<p>想打印该变量出来也很简单，用<code>for</code>循环就行</p>
<h2 id="结构体内存大小"><a href="#结构体内存大小" class="headerlink" title="结构体内存大小"></a>结构体内存大小</h2><p>这个内存大小涉及知识点较多，我会另外写一篇博客单独讲解。</p>
]]></content>
      <categories>
        <category>c/c++基础语法</category>
      </categories>
      <tags>
        <tag>c语言 算法</tag>
      </tags>
  </entry>
  <entry>
    <title>如何测试一个机器是大端还是小端</title>
    <url>/2023/10/18/%E5%A6%82%E4%BD%95%E6%B5%8B%E8%AF%95%E4%B8%80%E4%B8%AA%E6%9C%BA%E5%99%A8%E6%98%AF%E5%A4%A7%E7%AB%AF%E8%BF%98%E6%98%AF%E5%B0%8F%E7%AB%AF/</url>
    <content><![CDATA[<p>​	小端存储和大端存储的区别可能不太明显，但在处理二进制数据时会产生影响，特别是在不同计算机架构之间进行数据交换时。程序员和开发人员需要注意存储顺序，以确保数据在不同系统之间正确传递和解释。某些协议和文件格式规定了使用的字节顺序，以确保跨平台互操作性。</p>
<span id="more"></span>

<h2 id="什么是小端存储，大端存储"><a href="#什么是小端存储，大端存储" class="headerlink" title="什么是小端存储，大端存储"></a>什么是小端存储，大端存储</h2><p>  小端存储和大端存储都是计算机系统中用于组织和存储多字节数据的两种不同方法。</p>
<ol>
<li><p>小端存储：</p>
<ul>
<li>在小端存储中，<strong>最低</strong> 有效字节（即最小的地址部分）存储在内存的<strong>最低地址</strong>处，而<strong>最高</strong>有效字节（即最大的地址部分）存储在内存的<strong>最高地址</strong>处。</li>
</ul>
</li>
<li><p>大端存储：</p>
<ul>
<li>在大端存储中，<strong>最高</strong>有效字节（即最大的地址部分）存储在内存的<strong>最低地址处</strong>，而<strong>最低</strong>有效字节（即最小的地址部分）存储在内存的<strong>最高地址</strong>处。</li>
</ul>
</li>
</ol>
<h2 id="为什么会存在小端存储，大端存储"><a href="#为什么会存在小端存储，大端存储" class="headerlink" title="为什么会存在小端存储，大端存储"></a>为什么会存在小端存储，大端存储</h2><p>  小端存储和大端存储的存在是由计算机系统的设计和历史演变所决定的。这两种存储方式有各自的优点和用途，取决于特定的硬件和软件需求，其原因包括打不仅限于以下几点：</p>
<ol>
<li><p><code>历史原因</code>：计算机的存储方式早在计算机行业的早期就已经存在。IBM System&#x2F;360计算机系列是在1960年代早期首次引入的，它使用了大端存储方式。随着时间的推移，不同的计算机制造商采用了不同的存储方式。因此，存储方式的选择在很大程度上<strong>取决于计算机制造商的决策</strong>。</p>
</li>
<li><p><code>硬件架构</code>：不同的处理器架构对存储方式有不同的偏好。例如，Intel x86架构采用小端存储（现存x86机器大都也是小端存储），而一些其他处理器架构如SPARC和MIPS采用大端存储。这种差异主要是<strong>由硬件设计和指令集架构决定的</strong>。</p>
</li>
<li><p><code>数据传输</code>：大端存储方式在网络通信协议中更为常见，因为它<strong>与人类的阅读习惯（从左到右）和大多数书写系统更匹配</strong>。这就是为什么许多网络协议如TCP&#x2F;IP使用大端存储方式。小端存储方式<strong>在个别硬件上的数据传输速度更高</strong>，因为它更容易与硬件原语（如处理器的加载和存储指令）匹配。</p>
</li>
<li><p><code>多字节数据</code>：小端存储方式在多字节数据类型（如整数和浮点数）上执行特定操作时<strong>效率更高</strong>，因为最低有效字节是首先被访问的。这可以提高内存操作的效率，尤其在处理大型数据集时。</p>
</li>
</ol>
<h2 id="怎么判断本电脑是大端存储还是小端存储"><a href="#怎么判断本电脑是大端存储还是小端存储" class="headerlink" title="怎么判断本电脑是大端存储还是小端存储"></a>怎么判断本电脑是大端存储还是小端存储</h2><ol>
<li>我们可以编写一个简单的测试程序，用于检查处理器的字节顺序。</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">int</span> num = <span class="number">1</span>;</span><br><span class="line">    <span class="type">char</span> *c = (<span class="type">char</span>*)&amp;num;</span><br><span class="line">    <span class="keyword">if</span> (*c == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;小端存储 \n&quot;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;大端存储 \n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们创建一个无符号整数（num），然后将其地址强制转换为指向字符的指针（char *）。它检查最低地址处的字节的值，如果最低地址处的字节值为1，那么CPU采用小端存储；如果最高地址处的字节值为1，那么CPU采用大端存储。</p>
<p><img src="/./%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/23_10_22_1.png"></p>
<ol start="2">
<li>如果我们是用的 VS 编译器，我们可以打开一个内存的窗口，把列改为1，然后随便定义一个变量（a），按F11逐条编译，在内存窗口中输入 <code>&amp;a</code> 可以看到如图</li>
</ol>
<p><img src="/./%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/23_10_22_2.png"></p>
]]></content>
      <categories>
        <category>c/c++基础语法</category>
      </categories>
      <tags>
        <tag>c语言 算法</tag>
      </tags>
  </entry>
  <entry>
    <title>关于使用hexo创建博客</title>
    <url>/2023/10/15/%E5%85%B3%E4%BA%8E%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<p>​	初次创建属于自己的博客，完全是从零开始，在这期间遇到了许许多多的困难，光是把这个网站变成自己喜欢的样子都花了有半个月的样子，网上资料多又杂乱。而且这也是第一次写，不知道什么样的格式内容会受大家喜欢，干脆把自己踩过的坑，遇到的困难总结起来，把经验分享给大家，希望大家多多批评指正。</p>
<span id="more"></span>

<p><strong>前言</strong>：<br>我用的是win系统电脑，如果用的是水果电脑，可以参考  <a href="https://www.bilibili.com/video/BV1Yb411a7ty/?spm_id_from=333.999.0.0&vd_source=7cfb68109cfb644748c42234407548fb">手把手教你从0开始搭建自己的个人博客</a> ，其实两者大同小异，我会尽量用命令的方式去操作，大家可以直接<code>ctrl + c + v</code>跳过中间烧脑的原理部分</p>
<h2 id="1-前期准备"><a href="#1-前期准备" class="headerlink" title="1.前期准备"></a>1.前期准备</h2><p>1.下载git</p>
<ul>
<li>hexo 开源于 github，需要git来下载，这里就不赘述下载流程了，下载可以参考<a href="https://blog.csdn.net/mukes/article/details/115693833">Git 详细安装教程</a></li>
</ul>
<p>2.下载node</p>
<ul>
<li><a href="https://nodejs.org/en">点击跳转</a> 安装 <code>LTS</code> 版本的，一直下一步就可以了</li>
</ul>
<p>3.下载 <code>Notepad++</code> </p>
<ul>
<li>参考 [Notepad++ 下载安装和使用](<a href="https://blog.csdn.net/">https://blog.csdn.net/</a><br>louxiaofei_java&#x2F;article&#x2F;details&#x2F;118121569)</li>
</ul>
<h2 id="2-使用命令行搭建hexo框架"><a href="#2-使用命令行搭建hexo框架" class="headerlink" title="2.使用命令行搭建hexo框架"></a>2.使用命令行搭建hexo框架</h2><p>1.按键<strong>win + command</strong>打开命令提示符</p>
<p>2.输入命令查看node版本，确定node安装成功</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">node -v	</span><br></pre></td></tr></table></figure>
<p>同理查看npm版本</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm -v</span><br></pre></td></tr></table></figure>
<p><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/2.2.png"></p>
<p>3.因为是外网，没有魔法的朋友打开下载都会会很慢，我们利用一个淘宝的镜像来安装会更加便捷，输入命令</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install -g cnpm --registry=http://registry.npm.taobao.org</span><br></pre></td></tr></table></figure>

<p>安装后，检查安装是否成功</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cnpm -v</span><br></pre></td></tr></table></figure>
<p><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/2.3.png"></p>
<p>4.安装hexo框架</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">cnpm install -g hexo-cli</span><br></pre></td></tr></table></figure>
<p>查看是否成功</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">hexo -v</span><br></pre></td></tr></table></figure>
<p><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/2.4.png"></p>
<h2 id="3-创建本地Blog网站"><a href="#3-创建本地Blog网站" class="headerlink" title="3.创建本地Blog网站"></a>3.创建本地Blog网站</h2><p>1.首先在电脑找一个自己喜欢的位置创建一个名为<code>My_Blog</code>的文件夹，我们所有关于博客的东西都在这里面，如果中间出现一些解决不了的问题，可以直接把该文件夹干掉重来。</p>
<p>2.进入<code>My_Blog</code>的文件夹，<code>右击鼠标 - 显示更多 - Git Base Here</code><br> ，我们所有操作都可以在这个命令行里操作<br><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/3.2.png"></p>
<p>3.初始化博客<strong>（文件里必须为空）</strong>，这个过程比较慢，请耐心等待，输入命令</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo init</span><br></pre></td></tr></table></figure>
<p><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/3.3.png"></p>
<p>4.启动本地博客服务 <strong>（会生成一个网站）</strong>，用作预览</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo s</span><br></pre></td></tr></table></figure>
<p><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/3.4.png"><br>5.本地访问地址</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http://localhost:4000/</span><br></pre></td></tr></table></figure>



<blockquote>
<p>到此为止，我们就完成了大部分的博客搭建，进入网站就可以看见自己浩瀚星海蓝图的展开，接下来我们要把它上传到我的github上面，让它面向大众，让更多的小伙伴可以看到</p>
</blockquote>
<h2 id="4-创建github库，把本地文件上推"><a href="#4-创建github库，把本地文件上推" class="headerlink" title="4. 创建github库，把本地文件上推"></a>4. 创建github库，把本地文件上推</h2><p>1.我们首先要拥有一个GitHub账号，文章推荐<a href="https://blog.csdn.net/m0_67906358/article/details/128808210?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522169737483416800186593921%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=169737483416800186593921&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-128808210-null-null.142%5Ev96%5Epc_search_result_base6&utm_term=%E5%88%9B%E5%BB%BAgithub%E8%B4%A6%E5%8F%B7&spm=1018.2226.3001.4187">注册Github账号详细教程</a></p>
<p>2.新建一个库<br><strong>注意：</strong>名字必须写 “xxx(用户名).github.io” 的格式<br><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/4.2.png"><br>然后create就可以创建好一个库了</p>
<p>3.我们返回github base here ，在blog目录下安装git部署插件输入命令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
<p>没有红色的ERROR就是没问题的</p>
<p>4.回到My_Blog文件里，找到 <code>_config.yml </code> 文件，右击鼠标用notepad++编辑，_config.yml文件是我们以后操作博客最主要的文件<br><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/4.4.png"></p>
<p>5.拉到最下面，在depoly下按照如图填入<br><strong>注意：</strong>所有冒号后面都有空格。<br><img src="/./%E4%BD%BF%E7%94%A8hexo%E5%88%9B%E5%BB%BA%E5%8D%9A%E5%AE%A2_%E6%97%A5%E5%BF%97%E5%9B%BE%E7%89%87/4.5.png"><br>保存退出</p>
<p>6.需要你的邮箱和名称，输入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git config --global user.email “邮箱”</span><br><span class="line">git config --global user.name “名称”</span><br></pre></td></tr></table></figure>

<p>7.推到GitHub库中</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexo d</span><br></pre></td></tr></table></figure>
<p>一大串代码过后就成功了</p>
<p><strong>恭喜恭喜</strong><br>你已经成功创建了一个属于自己的博客，快输入网址https:&#x2F;&#x2F;（用户名）.github.io&#x2F; ，开启你的博客生涯吧！</p>
]]></content>
      <categories>
        <category>Blog</category>
      </categories>
      <tags>
        <tag>Blog</tag>
      </tags>
  </entry>
</search>
